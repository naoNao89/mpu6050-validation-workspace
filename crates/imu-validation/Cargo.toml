[package]
name = "imu-validation"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
imu-core = { version = "0.1.0", path = "../imu-core", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
